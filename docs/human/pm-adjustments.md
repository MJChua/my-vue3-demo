# 專案調整紀錄

這份文件用時間順序整理專案已完成或規劃中的重要調整。

## 01 Workspace 與共用 Library 基礎

- 統一使用 `pnpm`。
- 建立 `packages/core`、`packages/http`、`packages/ui`。
- 補上 roadmap 與可重用能力清單。

## 02 首頁 API 與圖片顯示修復

- 改用穩定的公開圖片 API。
- 修正首頁圖片清單顯示。
- 加強 URL 請求處理。

## 03 首頁 UI 與主題控制調整

- 統一圖片卡片尺寸。
- 將主題切換移到 header。
- 移除舊的 Mine 頁主題切換入口。

## 04 測試基礎

- 加入 Vitest 與 Vue Test Utils。
- 加入 MSW API mock。
- 加入 Playwright E2E。
- 加入 PR 自動檢查流程。

## 05 全站雙語與右側控制區

- 加入繁體中文與英文語系。
- 將主要頁面改用翻譯 key。
- 將主題與語言控制集中在 header 右側。

## 06 語系檔規則與主題對比修復

- 語系內容改放在 `src/i18n/messages/`。
- 使用 locale-pack 載入層管理語系資料。
- 修正深色/淺色模式下語言切換的可讀性。

## 07 LaceDB 唯讀模型與首頁體驗升級

- 定義寵物資料模型。
- 加入首頁 hero 視覺。
- 加入寵物牆、分類篩選與圖片失敗提示。

## 08 Vite 遷移與沉浸式首頁

- 從 Vue CLI 遷移到 Vite。
- 強化首頁 Hero、每日焦點、分類探索與上傳預告。
- 更新頁尾內容。

## 09 部署 SOP

- 補上部署指南。
- 加入上線前檢查、打包規則、展示網址與回復方案。

## 10 Release 與 Hotfix 分支規範

- 規定 release 分支命名。
- 規定 hotfix 分支命名。
- 明確定義 `release_* -> master -> deploy` 流程。

## 11 全站回到頂部按鈕

- 新增全站浮動按鈕。
- 捲動到指定距離後顯示。
- 支援平滑回到頂部與可及性標籤。

## 12 手機 Header 與 Footer 導覽重整

- 手機 header 改為左選單、中品牌、右登入入口。
- 手機 footer 改為五區導覽。
- 中間加號可展開新增日記與上傳圖片。
- 新增日記、登入、個人中心與更多頁骨架。

## 13 手機選單與使用者內容流程完成

- 調整手機抽屜中的主題與語言切換。
- 使用頭像作為 Mine 入口。
- 完成日記建立、圖片上傳與個人頁資料操作。
- 加強深色與淺色主題的文字對比。
